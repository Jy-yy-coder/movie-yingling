# Vercel serverless 运行时依赖（精简：无 pandas/uvicorn/sentence-transformers/numpy）
# - pandas 仅本地从 CSV 构建 lookup.db 时需要
# - sentence-transformers/torch 无法上 serverless，向量检索自动降级为关键词模式
# - numpy 仅本地构建向量（build_vectors/embed）使用，运行时无引用
fastapi>=0.110.0
openai>=1.0.0
